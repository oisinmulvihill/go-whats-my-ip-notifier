package publicip

import (
	"internal/assert"
	"testing"
)

func TestGetAddressOKCase(t *testing.T) {
	// // Given: a name to use
	// given := "Aerial"

	// // When: calling hello with this name
	// name, err := Hello(given)

	// // Then: no error occured and the correct message is returned
	// assertEqual(t, err, nil)
	// expected := "Hi, Aerial. Welcome!"
	assert.Equal(t, 1, 1)

	// if name != expected {
	// 	t.Errorf("Hello(%v) = %v; want %v", given, name, expected)
	// }
}
